function calc() {
    console.log(studentName.value, biology.value, physics.value, chemistry.value, math.value, geo.value, studentID.value, eng.value);

    if (studentName.value == '' && biology.value == '' && physics.value == '' && chemistry.value == '' && math.value == '' && geo.value == '' && studentID.value == '' && eng.value == '') {
        alert("Boda wa, haba")
    } else {
        alert("Go on bro")
    }
}